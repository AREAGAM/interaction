# SEE / SHOW DESIGN STUDIO 本地完整包

## 直接查看

Windows 用户解压完整文件夹后，双击 `一键本地预览.cmd`。浏览器会打开 `http://127.0.0.1:4174/`，关闭命令窗口即可停止本地网页。

这个入口直接读取已经构建完成的 `dist`，不需要安装 Node.js，也不依赖互联网。不要直接双击 `dist/index.html`，浏览器会阻止三维模型和模块文件的正常载入。

## 修改网站内容

用 Excel 打开 `public/content/site-content.xlsx`。工作簿包含“网站设置”“分类设置”和“项目内容”：

- 修改文字后保存，打开中的本地网页约 2 秒内自动刷新。
- 本地图片放进 `public/images`，在“图片地址”填写 `/images/文件名`。
- “视频地址”支持 YouTube、Vimeo、Bilibili 页面链接，以及 mp4、webm、ogg 直链。
- 项目编号 X-001 至 X-048 不要修改；六个分类各保留八个项目。

在线网站不会读取你电脑里的文件。修改完成后需要重新构建并发布，线上地址才会更新。

## 开发模式

需要 Node.js 22.12 或更高版本。双击 `启动作品集.cmd`，或者运行 `npm ci` 后再运行 `npm run dev`。修改代码或模型后运行 `npm run build`，即可更新 `dist` 中的本地成品。

## 模型文件

- `art/rhine-archive.blend`：阵列和抽取模型的 Blender 源文件。
- `art/archive-assembly.blend`：360° 拆解模型的 Blender 源文件。
- `public/assets/archive-cassette.glb`：网页阵列模型。
- `public/assets/archive-assembly.glb`：网页拆解模型。
- `art/build_archive.py`、`art/build_assembly.py`：可重复生成脚本。

当前归档版本的 Git 标签为 `seeshow-blackline-backup-2026-09-10`。后续模型修改不满意时，可以从这个标签恢复当前版本。
